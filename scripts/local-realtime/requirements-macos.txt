# macOS Apple Silicon：本地 Qwen3-TTS + Whisper + 实时服务
# 由 setup-qwen3-tts.sh / 桌宠自动配置写入 Application Support 的 venv。
#
# 注意：不要在这里固定 transformers / huggingface_hub 的版本——
# mlx-audio 0.4.4 要求 huggingface_hub>=1.0 且 transformers>=5.5.0；
# 之前固定 transformers==4.57.6 + huggingface_hub==0.36.2 会直接
# 触发 pip ResolutionImpossible。代码里也没人直接 import 它们，
# 让 pip 根据 mlx-* 三个核心包自动解析依赖即可。
mlx-audio==0.4.4
mlx-lm==0.31.3
mlx-whisper
websockets>=12.0
soundfile==0.14.0
numpy>=2.0
